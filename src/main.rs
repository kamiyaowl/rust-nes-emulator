extern crate rust_nes_emulator as emu;

use emu::nes::system::{System};
/* for desktop simulation driver */
// TODO: ある程度完成したらDesktopで動かす部分は別リポジトリに移動して、本リポジトリをライブラリとして参照する
/* for desktop simulation driver */

fn main() {
    let sys: System;
    println!("Hello, world!");
}
